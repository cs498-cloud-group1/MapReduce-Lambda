language: node_js
cache:
  directories:
  - node_modules
install:
  - yarn install
  - npm install -g serverless
  - npm install -g serverless-offline
deploy:
  - provider: script
    script: yarn deploy
    on:
      branch: master